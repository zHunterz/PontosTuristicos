<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tg2.com.local.pauloafonso.pontosturisticos">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app_patur"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_patur"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CadastroUsuarioActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.BelvedereActivity" />
        <activity android:name=".activity.CatarinaActivity" />
        <activity android:name=".activity.UmbuzeiroActivity" />
        <activity android:name=".activity.ComplexoActivity" />
        <activity android:name=".activity.MangueiraActivity" />
        <activity android:name=".activity.MariaBonitaActivity" />
        <activity android:name=".activity.MochilaActivity" />
        <activity android:name=".activity.CaniosActivity" />
        <activity android:name=".activity.CachoeiraActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.BelvedereMapActivity"
            android:label="@string/title_activity_belvedere_map" />
        <activity android:name=".activity.lazerActivity"></activity>
    </application>

</manifest>